# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=ecoclean_db

# Server Configuration
PORT=3000

# CORS: set to your frontend origin in production (e.g. https://yourdomain.com). Leave empty for * (development).
# CORS_ORIGIN=
# Rate limit: max POST /api/messages per IP per 15 minutes (default 20)
# RATE_LIMIT_MESSAGES_MAX=20

# Email Configuration (SMTP) - Gmail
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=softionyxgroup@gmail.com
SMTP_PASS=zqodavcuknwiykky
CONTACT_EMAIL=softionyxgroup@gmail.com
